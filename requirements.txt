# Runtime lock: GitHub Actions ubuntu-24.04 / Python 3.11.15
requests==2.34.2
PyYAML==6.0.3
# requests transitive dependencies pinned for reproducible runner installs.
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0

# First-party IANA time-zone database fallback for Windows / minimal runtimes.
tzdata==2026.3
