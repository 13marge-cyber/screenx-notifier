# Python/runtime caches
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Local secrets/configs
.env
config.yaml

# OS/editor junk
.DS_Store
*.swp

# Atomic state leftovers only; notifier state JSON itself is intentionally tracked.
data/notifier-state/.state*.tmp
data/notifier-state/.probe-*
